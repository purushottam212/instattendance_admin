import 'package:flutter/material.dart';

class ClassesManagement extends StatelessWidget {
  const ClassesManagement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Classes Management'),
      ),
    );
  }
}
