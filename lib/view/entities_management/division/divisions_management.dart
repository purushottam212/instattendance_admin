import 'package:flutter/material.dart';

class DivisionsManagement extends StatelessWidget {
  const DivisionsManagement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Divisions Management'),
      ),
    );
  }
}
